const SLICE=10;const vueApp=new Vue({data:{types:[],records:null,tags:[],today:moment().format("YYYY-MM-DD"),options:{type:options.type||"",date:options.date,q:options.q,tag:options.tag,slice:options.slice||SLICE,},},methods:{addRecord(data){return DB.addRecord(data).then(record=>this.records.unshift(record)).then(()=>this.update());},removeRecord(record_id){return DB.delRecord(record_id).then(()=>{index=this.records.findIndex(data=>data.id==record_id);record=this.records[index];this.records.splice(index,1);return record;}).then(record=>this.removeUnusedTags(record.tags)).then(()=>this.update());},update(){this.records=null;date=this.date_time?moment(this.date_time).format("YYYY-MM-DD"):null;DB.getTypes().then(types=>this.types=types);DB.getTags().then(tags=>this.tags=tags);DB.getRecords(this.options).then(records=>this.records=records);},getComponent:id=>getComponent(id),getRecordDate:record=>moment(record.date+"T"+record.time).format("LLL"),getFormattedDate:date=>moment(date).format("LL"),getDateUrl:date=>`/date.html?date=${moment(date).format("YYYY-MM-DD")}`,getTagUrl:tag=>`/tag.html?tag=${tag.name}`,removeUnusedTags(tags){return DB.removeUnusedTags(tags).then(()=>this.update());},},});if(options.type&&options.page){DB.getType(options.type).then(type=>document.querySelector("#app").innerHTML=type.page.template).then(()=>vueApp.$mount("#app")).then(vueApp.update)}else{vueApp.$mount("#app");vueApp.update();}