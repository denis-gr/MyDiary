const SLICE=10;const vueApp=new Vue({data:{types:[],records:null,tags:null,today:moment().format("YYYY-MM-DD"),options:{type:options.type||"",date:options.date,q:options.q,tag:options.tag,slice:options.slice||SLICE,},},methods:{async addRecord(data){record=await DB.addRecord(data);this.records.unshift(record);await this.update();},async removeRecord(record_id){await DB.delRecord(record_id);index=this.records.findIndex(data=>data.id==record_id);record=this.records[index];this.records.splice(index,1);await this.removeUnusedTags(record.tags);await this.update();},async update(){this.records=this.tags=null;date=this.date_time?moment(this.date_time).format("YYYY-MM-DD"):null;this.types=await DB.getTypes();this.tags=await DB.getTags();this.records=await DB.getRecords(this.options);},async removeUnusedTags(tags){await DB.removeUnusedTags(tags);await this.update();},getComponent:id=>getComponent(id),getRecordDate:record=>moment(record.date+"T"+record.time).format("LLL"),getFormattedDate:date=>moment(date).format("LL"),getDateUrl:date=>`/diary/date.html?date=${moment(date).format("YYYY-MM-DD")}`,getTagUrl:tag=>`/diary/tag.html?tag=${tag.name}`,},created(){this.update();}});DB.getType(options.type).then(type=>{if(type&&type.page){document.querySelector("#page").innerHTML=type.page.template;}else{document.querySelector("#page .load").hidden=true;document.querySelector("#page .errors").hidden=false;};}).then(()=>vueApp.$mount("#app"));